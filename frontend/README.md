# Frontend

React + TypeScript notes UI for Parking Lot, ready to deploy on Vercel.

## Development

Run:

```bash
npm install
npm run dev
```

If PowerShell blocks `npm`, use:

```bash
cmd.exe /c npm.cmd install
cmd.exe /c npm.cmd run dev
```

## Deployment

In Vercel, use `frontend` as the project root.
